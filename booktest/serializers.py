from rest_framework import serializersfrom .models import BookInfoclass BookInfoModelSerializer(serializers.ModelSerializer):    class Meta:        model = BookInfo  # 指定序列化从哪个模型类映射        fields = '__all__'  # 指明模型类要生成的那些字段        # fields = ['id', 'btitle', 'bpub_date', 'bread', 'bcomment']  # 指明模型类要生成的那些字段        # exclude = ('image',)  # 明确排除掉那些字段        # read_only_fields = ('bcomment',)  # 指明只读字段，仅用于序列化输出的字段        # extra_kwargs = {        #     'bread': {'min_value': 0, 'required': True},        #     'bcomment': {'min_value': 0, 'required': True},        # }